- if record.errors.any?
  .alert.alert-danger.alert-dismissable
    button.close data-dismiss="alert" ×
    h4 入力に問題があります
    ul
      - errors = record.errors.map { |atr, err| [atr, err] }
      - errors.uniq.each do |atr, error|
        li = record.errors.full_message(atr, error)
