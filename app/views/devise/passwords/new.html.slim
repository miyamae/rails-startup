- @title = t 'views.devise.passwords.new.title'
- add_breadcrumb t('views.devise.sessions.new.title'), new_user_session_path
= t 'views.devise.passwords.new.guide_html'
= render 'partial/errors', record: resource
= form_for(resource, as: resource_name, url: password_path(resource_name), html: { method: :post, class: 'form-horizontal' }) do |f|
  = f.bootstrap_text :email
  .form-group
    .col-sm-offset-3.col-sm-9
      button.btn.btn-primary type="submit"
        = t 'views.devise.passwords.new.submit'
